# ============================================================
# Minimal requirements for the desktop (SQLite, no Docker) build.
# This is what gets bundled into the PyInstaller executable.
# The full backend/requirements.txt (Postgres + Redis + Celery) is for
# the Docker/server deployment - keep both in sync on shared packages.
# ============================================================

fastapi==0.116.1
uvicorn[standard]==0.35.0

sqlalchemy==2.0.43

pydantic==2.11.7
pydantic-settings==2.10.1

python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

python-multipart==0.0.20

python-dateutil==2.9.0.post0
email-validator==2.3.0
python-dotenv==1.1.1

# Packaging
pyinstaller==6.11.1
pyinstaller-hooks-contrib==2024.10
